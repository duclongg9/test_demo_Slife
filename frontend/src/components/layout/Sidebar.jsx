/** Mục đích: Bộ lọc (category, price, condition, purpose). API: không gọi trực tiếp, phát sự kiện lên page/hook. */
import { Box, Typography } from '@mui/material';
export default function Sidebar(){ return <Box minWidth={240}><Typography>TODO: Filter panel skeleton</Typography></Box>; }
