/**
 * Mục đích: Skeleton test cho component ListingForm.
 * TODO: kiểm tra render, tương tác người dùng, và trạng thái loading/lỗi.
 */
import { describe, it, expect } from 'vitest';
import ListingForm from '../listing/ListingForm';

describe('ListingForm', () => {
  it('TODO: render cơ bản', () => {
    expect(ListingForm).toBeTruthy();
  });

  it('TODO: interaction/API mock', () => {
    expect(true).toBe(true);
  });
});
