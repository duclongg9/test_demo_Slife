/** Mục đích: loading placeholder chung. */
import { CircularProgress, Stack } from '@mui/material';
export default function Loading(){ return <Stack alignItems='center' p={2}><CircularProgress /></Stack>; }
