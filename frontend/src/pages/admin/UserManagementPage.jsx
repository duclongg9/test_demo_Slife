/** Mục đích: Quản lý user/block/unblock. API: GET /api/admin/users, PATCH /api/admin/users/{id}/status. */
import { Box, Typography } from '@mui/material';
export default function UserManagementPage(){ return <Box><Typography>TODO: User management skeleton</Typography></Box>; }
