/** Mục đích: Trang hồ sơ user, listing, reviews, follow count/settings. API: GET /api/users/{id}, GET /api/users/{id}/listings. */
import { Box, Typography } from '@mui/material';
export default function ProfilePage(){ return <Box><Typography>TODO: Profile skeleton</Typography></Box>; }
