/** Mục đích: Chi tiết deal + pickup_time + send reminder. API: GET /api/deals/{id}, POST /api/deals/{id}/reminder. */
import { Box, Typography } from '@mui/material';
export default function DealDetailPage(){ return <Box><Typography>TODO: Deal detail skeleton</Typography></Box>; }
