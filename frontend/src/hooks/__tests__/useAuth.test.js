/** Test skeleton cho hook useAuth. */
import { describe, it, expect } from 'vitest';

describe('useAuth', () => {
  it('TODO: hành vi chính + mock axios/api', () => {
    expect(true).toBe(true);
  });
});
