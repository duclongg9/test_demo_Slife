/** Test skeleton cho hook usePagination. */
import { describe, it, expect } from 'vitest';

describe('usePagination', () => {
  it('TODO: hành vi chính + mock axios/api', () => {
    expect(true).toBe(true);
  });
});
