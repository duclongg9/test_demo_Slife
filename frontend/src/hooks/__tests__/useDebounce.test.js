/** Test skeleton cho hook useDebounce. */
import { describe, it, expect } from 'vitest';

describe('useDebounce', () => {
  it('TODO: hành vi chính + mock axios/api', () => {
    expect(true).toBe(true);
  });
});
