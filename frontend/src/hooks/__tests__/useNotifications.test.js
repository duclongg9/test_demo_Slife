/** Test skeleton cho hook useNotifications. */
import { describe, it, expect } from 'vitest';

describe('useNotifications', () => {
  it('TODO: hành vi chính + mock axios/api', () => {
    expect(true).toBe(true);
  });
});
