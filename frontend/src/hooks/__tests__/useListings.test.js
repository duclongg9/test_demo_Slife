/** Test skeleton cho hook useListings. */
import { describe, it, expect } from 'vitest';

describe('useListings', () => {
  it('TODO: hành vi chính + mock axios/api', () => {
    expect(true).toBe(true);
  });
});
